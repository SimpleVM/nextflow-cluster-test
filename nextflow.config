profiles {
    slurm {
        docker {
           enabled = true
        }

        process {
           executor = 'slurm'
        }
    }

    local {
         docker {
           enabled = true
         }

         process {
           executor = 'local'
         }
     }
}
